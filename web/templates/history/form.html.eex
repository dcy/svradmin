<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below:</p>
      <ul>
        <%= for {attr, message} <- f.errors do %>
          <li><%= humanize(attr) %> <%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :who, "Who", class: "control-label" %>
    <%= text_input f, :who, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= label f, :what, "What", class: "control-label" %>
    <%= number_input f, :what, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= label f, :svr_id, "Svr", class: "control-label" %>
    <%= number_input f, :svr_id, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
